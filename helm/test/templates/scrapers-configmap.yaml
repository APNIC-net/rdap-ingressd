apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "application.fullname" . }}-scrapers
  labels:
    app: {{ template "application.fullname" . }}-scrapers
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
data:
 {{ range $path, $_ :=  .Files.Glob  "scrapers/**.json" }}
   {{ base $path }}: |
{{ $.Files.Get $path  | indent 4}}
 {{ end }}